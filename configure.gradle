task configure {
    doLast {
        def pos = configure(new Model()) {
            name = 10
            age = 50
        }
        println pos.name
        println pos.age
    }
}

class Model{
	def name;
	def age;
}