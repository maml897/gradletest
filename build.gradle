plugins {
    id 'base'
}

task myJar(type: Jar)
artifacts {
    archives (file('build/somefile.txt')){
        name 'my-artifact'
        type 'text'
        builtBy myJar
    }
}

//base插件声明配置，base默认创建了两个configurations 分别是default和archives。testok是我们自己建立的。
configurations{
	testok
}

//这里我们配置 default，archives，testok
artifacts {
    
}

repositories {
    jcenter()
}

